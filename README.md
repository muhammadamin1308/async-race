# async-race
npm start

Check for linting errors:
npm run lint

Fix linting errors automatically:
npm run lint:fix

Format your code:
npm run format

Start the Server:
json-server db.json --port 3000

